'use strict';

/**
 * @ngdoc function
 * @name uiRouterAnimationApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the uiRouterAnimationApp
 */
angular.module('uiRouterAnimationApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
